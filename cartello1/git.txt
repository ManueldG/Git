git init
git clone http://indirizzo
git add . | aggiunge tutti i file 
git commit-m"testo" | commit con messaggio

git checkout [yourbranchname]  | muovo HEAD in posizione del label
git checkout -b [yourbranchname] | crea e seleziona il branch (-b)

git cherry-pick 

git add . | aggiunge tutti i file 

git commit-m"testo" | commit con messaggio

git branch [yourbranchname] | creo nuovo branch
git branch -f [yourbranchname]  C2' | forzo (-f) lo spostamento del commit puntato da pushed in posizione C2

git cherry-pick 

git add . | aggiunge tutti i file 

git rebase -i HEAD~5 | copio i rami a partire dalla posizione selezionata "~n" indica quanti passi n tornare indietro dalla posizione -i interactive
git merge [yourbranchname] | collega il branch al HEAD